In backend folder
pip install flask flask-cors
pip install pymongo

run the server in the backend folder:
python app.py


In frontend folder
npm install

run the server in the frontend folder
npm start

